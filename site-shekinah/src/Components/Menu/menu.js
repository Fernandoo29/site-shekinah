import styles from './menu.module.css'

export default function Menu(){
    return (
        <header id={styles.menu}>
            <div id={styles.logo}>
                logo
            </div>
        </header>
    )
}